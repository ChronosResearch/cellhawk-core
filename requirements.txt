# ── Runtime ──────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0
celery[redis]>=5.4.0
redis[hiredis]>=5.0.0
protobuf>=5.27.0
pydantic>=2.7.0
torch>=2.3.0
numpy>=1.26.0
pyyaml>=6.0.1
structlog>=24.2.0
prometheus-client>=0.20.0
# Geospatial (terrain ingestion)
shapely>=2.0.0
# MAVLink (PX4 adapter)
pymavlink>=2.4.41
# Cryptography (AES-256-GCM + mTLS, Gap 7)
cryptography>=42.0.0

# ── Dev / Test ────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0
ruff>=0.4.0
mypy>=1.10.0
types-PyYAML>=6.0.12
types-redis>=4.6.0
maturin>=1.4.0
